name: Build

on:
  pull_request:
  push:
    branches:
      - main
      - nWEIdia-patch-1
# cancels concurrent in-progress runs of the pull request, if not a PR, do not cancel
# pushes to main branch will generate an uniq run_id, PR.number will be
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id}}
  cancel-in-progress: true

env:
  working_directory: .

jobs:
  clang-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Build with clang++
        working-directory: ${{ env.working_directory }}
        run: |
          tools/apt-install-things.sh &
          tools/pip-install-things.sh &
          source tools/setup-env.sh
          export CC=clang
          export CXX=clang++
          wait
          python setup.py build

  dynamic-type-meson:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Run meson build of dynamic type library
        working-directory: ${{ env.working_directory }}
        run: |
          sudo apt update && sudo apt install -y libgtest-dev libgmock-dev libbenchmark-dev &
          pip install git+https://github.com/mesonbuild/meson ninja &
          wait
          cd lib/dynamic_type
          meson setup build
          meson compile -C build
          meson test --verbose -C build
